---
phase: 01-robust-core-progress
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/ActiveRunView.tsx
  - src/components/ErrorBanner.tsx
  - src/pages/Dashboard.tsx
autonomous: true
requirements:
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - User sees a responsive progress bar during active runs.
    - User is presented with clear error messages if backup fails.
  artifacts:
    - path: src/components/ActiveRunView.tsx
      provides: Progress and indeterminate state rendering
    - path: src/components/ErrorBanner.tsx
      provides: Critical error display and unlock action
  key_links:
    - from: src/pages/Dashboard.tsx
      to: src/components/ActiveRunView.tsx
      via: Rendered when active run exists
---

<objective>
Implement the primary visual feedback mechanisms for a running backup and clearly communicate failures or lock issues to the user.

Purpose: To give users transparency into the backup process and straightforward recovery paths when things go wrong.
Output: Active Run View with progress bar, an Error Banner, and their integration into the Dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robust-core-progress/01-CONTEXT.md
@.planning/phases/01-robust-core-progress/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Active Run View Component</name>
  <files>src/components/ActiveRunView.tsx</files>
  <action>
    Create `ActiveRunView` that pulls state from `useAppStore`.
    - Render `ProgressBar` with percentage. Show files/bytes stats.
    - Implement an "Indeterminate" loading state: If `runState === 'running'` but `progressStats` haven't updated for >2s, show an animated indeterminate bar (or pulse the existing bar) to indicate background activity continues despite missing JSON logs.
    - Add an "Advanced Details" toggle (button or accordion) that conditionally renders the `logs` array in a fixed-height, scrollable pre/code block.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ActiveRunView correctly renders progress state and hides granular logs behind a toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Error Banner Component</name>
  <files>src/components/ErrorBanner.tsx</files>
  <action>
    Create `ErrorBanner` displayed when `runState` is 'failed' or 'locked'.
    - Use the destructive color (`#f87171`) for error text or border per `01-UI-SPEC.md`.
    - Display the `errors` list gracefully.
    - If `runState === 'locked'`, prominently display an "Unlock Repository" button that calls `unlockRepo(activeProfileId)`. Once complete, call `clearRun()` or reset to 'idle'.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ErrorBanner correctly formats errors and provides a functional Unlock button.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Dashboard</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Update `Dashboard` to render `ActiveRunView` and `ErrorBanner` above the profiles list if there's an active run or a failed run (`activeRunId` is not null).
    When a user clicks "Run now" on a profile card, wait for `runBackup(p.id)` to return the `runId`, then immediately call `startRun(runId, p.id)` in the global store to trigger the UI switch.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Clicking run directly links into the ActiveRunView UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Restic output -> UI | Restic logs are rendered in the Advanced view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-1 | Information Disclosure | `ActiveRunView` | mitigate | Ensure raw logs are only visible behind the Advanced Details toggle to avoid overwhelming users, but don't sanitize out path names as they are necessary for debugging. |
</threat_model>

<verification>
Verify UI matches `01-UI-SPEC.md` colors (`#f87171` for destructive, `#34d399` for accent). Test that the Dashboard successfully switches to showing progress when a backup runs.
</verification>

<success_criteria>
- Active run view displays smoothly and falls back to indeterminate if stalled.
- Error banner successfully captures failure messages.
- "Unlock Repository" button is available exclusively when locked.
</success_criteria>

<output>
Create `.planning/phases/01-robust-core-progress/01-02-SUMMARY.md` when done
</output>