---
phase: 01-robust-core-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/restic/cli.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/main.rs
  - src/lib/tauri.ts
  - src/store/index.ts
  - src/hooks/useBackupManager.ts
  - src/App.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - User can trigger an unlock command from the frontend.
    - Global state tracks active run progress, logs, and locked status.
  artifacts:
    - path: src-tauri/src/commands.rs
      provides: unlock_repo command implementation
    - path: src/hooks/useBackupManager.ts
      provides: Throttled event listening for backup progress
    - path: src/store/index.ts
      provides: Global state for activeRunId and runState
  key_links:
    - from: src/hooks/useBackupManager.ts
      to: src/store/index.ts
      via: Zustand store updates
---

<objective>
Implement the backend foundation for unlocking repositories and the global frontend state for tracking active backup runs, including throttled event listening to prevent UI freezing.

Purpose: Lays the plumbing for the responsive progress bar, error handling, and single-run enforcement.
Output: Rust commands for unlocking, updated Zustand store, and a global hook tracking progress.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robust-core-progress/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unlock_repo to Tauri backend</name>
  <files>src-tauri/src/restic/cli.rs, src-tauri/src/commands.rs, src-tauri/src/main.rs, src/lib/tauri.ts</files>
  <action>
    - In `cli.rs`, add `pub fn unlock(profile: &BackupProfile) -> Self` that builds the `unlock` restic command (similar to `run_check`).
    - In `commands.rs`, add a new command `#[tauri::command] pub async fn unlock_repo(state: State<'_, AppState>, profile_id: String) -> Result<String, String>` that retrieves the profile, resolves its password, and executes the unlock command via `run_to_completion`.
    - Register `unlock_repo` in the `invoke_handler` in `main.rs`.
    - In `src/lib/tauri.ts`, add and export `export async function unlockRepo(profileId: string): Promise<string>` wrapping the `invoke("unlock_repo", { profileId })`.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>unlock_repo command is fully implemented and exported to the frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Global State for Active Run</name>
  <files>src/store/index.ts</files>
  <action>
    Update `useAppStore` to track active backup runs. 
    Add fields: `activeProfileId: string | null`, `activeRunId: string | null`, `runState: 'idle' | 'running' | 'locked' | 'failed'`, `progressStats` (object storing percent, files done, speed), `logs: string[]`, `errors: string[]`. 
    Add setter actions: `startRun(runId, profileId)`, `updateProgress(stats)`, `addLog(log)`, `addError(error)`, `setRunState(state)`, `clearRun()`. 
    Ensure the state defaults to 'idle'.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Zustand store exports the necessary state and actions for tracking a backup.</done>
</task>

<task type="auto">
  <name>Task 3: Backup Manager Hook</name>
  <files>src/hooks/useBackupManager.ts, src/App.tsx</files>
  <action>
    Create a custom hook `useBackupManager` that listens to `backup-progress` and `backup-complete` events via `useResticEvent` from `src/hooks/useResticEvents.ts`.
    - Retrieve `activeRunId` and state setters from `useAppStore`.
    - Throttle progress updates to ~500ms to avoid React re-render spam (e.g., using lodash throttle or a custom ref-based throttle).
    - On `backup-complete`, if exit code is non-zero, check `errors` array in state. If any error string contains "lock" or "locked by another process", set `runState` to 'locked'. Otherwise set to 'failed'. On success, clear the run or keep it briefly before returning to 'idle'.
    - Mount this hook globally inside `App.tsx` (under the router provider) so events are caught regardless of which page the user is viewing.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Progress events correctly update the global store at throttled intervals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Local FS | `unlock_repo` executes restic with repo paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-1 | Tampering | `unlock_repo` command | mitigate | Profile ID is parsed as UUID, backend retrieves pre-verified config from store. No raw input goes to CLI arguments. |
</threat_model>

<verification>
Ensure backend compiles and `unlock` parses correctly without breaking existing Tauri commands. Check that the Zustand store typescript interfaces are correctly typing the `progressStats`.
</verification>

<success_criteria>
- The rust backend can trigger `restic unlock`.
- `useAppStore` tracks active runs, avoiding race conditions.
- Progress updates are successfully throttled.
</success_criteria>

<output>
Create `.planning/phases/01-robust-core-progress/01-01-SUMMARY.md` when done
</output>