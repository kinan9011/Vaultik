---
phase: 01-robust-core-progress
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/Shell.tsx
  - src/pages/Dashboard.tsx
autonomous: false
requirements:
  - CORE-01
must_haves:
  truths:
    - User can switch active profiles using a prominent selector.
    - Multiple parallel runs are prevented by disabling run actions.
  artifacts:
    - path: src/components/Shell.tsx
      provides: Profile dropdown in Sidebar or TopBar
  key_links:
    - from: src/components/Shell.tsx
      to: src/store/index.ts
      via: Reads runState to disable selection
---

<objective>
Implement global profile switching and enforce application-wide safety by preventing concurrent backup runs.

Purpose: To ensure the UI accurately reflects a selected profile and blocks complex parallel run scenarios.
Output: Profile selector integration and UI lockouts during active operations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robust-core-progress/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile Selector in Shell</name>
  <files>src/components/Shell.tsx</files>
  <action>
    Update `Sidebar` or `TopBar` (developer discretion based on layout fit) to include a prominent Profile Dropdown/Select component that allows the user to explicitly switch the globally selected profile (`activeProfileId` in `useAppStore`).
    Ensure it gracefully handles the case where `profiles` is empty.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The user can select an active profile globally.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Single Run Safety</name>
  <files>src/components/Shell.tsx, src/pages/Dashboard.tsx</files>
  <action>
    Read `runState` from `useAppStore`.
    - In `Shell.tsx`, disable the profile dropdown selector if `runState === 'running'` to prevent switching context mid-run.
    - In `Dashboard.tsx`, disable all "Run Backup" and "Run now" buttons across all profile cards if `runState === 'running'`. Use visual cues (opacity, disabled attributes, `cursor-not-allowed`) to indicate they are blocked.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>It is impossible to start a new backup while another is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profile selector and active run blocking.</what-built>
  <how-to-verify>
    1. Open the application.
    2. Start a backup on a profile.
    3. Verify the profile dropdown is disabled.
    4. Verify other profiles' "Run now" buttons are disabled.
    5. Verify progress shows properly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Backend | Active profile ID selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-1 | Denial of Service | Run enforcement | accept | Local app; blocking concurrent runs prevents state/repo corruption but is enforced strictly client-side. Server-side `CancelRegistry` also protects against orphaned processes. |
</threat_model>

<verification>
Run `npm test` or manual verification checking the lock behavior on the buttons.
</verification>

<success_criteria>
- Global dropdown cleanly switches context.
- UI strictly enforces single active runs.
</success_criteria>

<output>
Create `.planning/phases/01-robust-core-progress/01-03-SUMMARY.md` when done
</output>